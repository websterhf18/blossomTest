/**
 * @format
 */

import 'react-native';
import React from 'react';
import App from '../App';

// Note: test renderer must be required after react-native.
import renderer from 'react-test-renderer';

/**it('renders filter correctly', () => {
  //renderer.create(<App />);
});
it('renders numbers filters correctly', () => {
  //renderer.create(<App />);
});**/
